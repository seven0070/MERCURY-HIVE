version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: mercury
      POSTGRES_PASSWORD: hivepassword
      POSTGRES_DB: mercury_hive
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
